apiVersion: v1
kind: Service

metadata:
  name: tetris-service
  labels:
    app: tetris-app

spec:
  selector:
    app: tetris-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      
  type: ClusterIP