apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tetris-ingress
spec:
  rules:
    - host: myapp-staging.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tetris
                port:
                  number: 80